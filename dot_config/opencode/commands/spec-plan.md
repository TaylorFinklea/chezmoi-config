---
description: Write a product and implementation spec
agent: spec-planner
---

Create a decision-complete product and implementation spec for:

`$ARGUMENTS`

Read repo instructions and `.docs/ai/` handoff state first. Inspect relevant files and configs. Ask only for product intent or tradeoffs that cannot be discovered. Prefer writing `.docs/ai/phases/<slug>-spec.md` when the repo uses `.docs/ai/`. Do not change product code.
