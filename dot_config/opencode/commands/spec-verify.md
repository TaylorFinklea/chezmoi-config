---
description: Verify implementation against a spec
agent: spec-verifier
---

Verify the current implementation against this spec:

`$ARGUMENTS`

Read repo instructions, the spec, recent commits, `git status`, and the relevant diff. Map every acceptance criterion to evidence or a concrete gap. Run the spec's verification commands when safe. Return a Pass, Partial, or Fail verdict with evidence and residual risk.
