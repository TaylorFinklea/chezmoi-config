---
description: Implement an approved spec
agent: spec-implementer
---

Implement the approved spec:

`$ARGUMENTS`

Read repo instructions, current handoff state, the full spec, and referenced files. Implement only the spec. Run the verification commands from the spec, update handoff docs and the phase report when appropriate, and create a small local commit. Do not push.
