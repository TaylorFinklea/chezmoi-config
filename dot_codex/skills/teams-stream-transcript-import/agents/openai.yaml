interface:
  display_name: "Teams Stream Import"
  short_description: "Import unseen Teams meeting transcripts"
  default_prompt: "Use $teams-stream-transcript-import to download unseen Teams meeting transcripts from Stream and ingest them into this Codex workspace."
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Playwright MCP browser automation"
