---
name: process-backlog
description: Execute Haiku and Sonnet backlog items from the roadmap while preserving the shared claim and verification protocol.
---

# Process Backlog

Follow the canonical workflow in `../../../docs/ai-workflows/process-backlog.md`.

Copilot-specific notes:

- This command is lower-tier only. It never executes Opus/T3 work.
- If the user wants architect-level work, use `/process-backlog-opus` instead.
- Keep implementation tightly scoped to the selected backlog item.
