# AI Config Inbox

Use this directory as the staging area for local AI artifacts discovered on a machine
but not yet accepted into the managed repo.

Each inbox entry should contain:

- `metadata.json` with `scope`, `targets`, and `kind`
- `source/` with the staged artifact content
- optional notes for the follow-up classification decision
